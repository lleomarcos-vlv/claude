# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock            # app is part of a monorepo; lock is generated per-machine
.metadata

# IDE
.idea/
.vscode/
*.iml

# Env / secrets — API keys are injected via --dart-define, never committed
env.json
*.env
android/key.properties
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
ios/Flutter/Generated.xcconfig

# OS
.DS_Store

# NOTE: keep assets/**/.gitkeep tracked so the per-flavor asset dirs exist.
