# OSC Scraper — dependências
# Instale com:  pip install -r requirements.txt  &&  playwright install chromium

# HTTP assíncrono (cliente da API)
httpx[http2]>=0.27,<0.29

# Automação de navegador (modo fallback / auto-discovery)
playwright>=1.44

# Exportação Excel
openpyxl>=3.1

# Manipulação/normalização de dados tabulares
pandas>=2.1

# Interface web (painel de controle)
fastapi>=0.111
uvicorn[standard]>=0.30

# Utilidades
python-dateutil>=2.9
rich>=13.7        # logging colorido (opcional, degrada com elegância se ausente)

# Testes
pytest>=8.0
pytest-asyncio>=0.23
